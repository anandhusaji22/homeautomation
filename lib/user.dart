import 'package:flutter/material.dart';

class user22 extends StatelessWidget {
  const user22({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(body: Text('welcom',style: TextStyle(fontSize: 30,fontWeight: FontWeight.bold)),);
  }
}