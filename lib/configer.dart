import 'package:flutter/material.dart';

class configer4 extends StatelessWidget {
  const configer4({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(appBar: AppBar(),
    
    body:  Container(  child: Center(child: const Text('confer',style: TextStyle(fontSize: 30,fontWeight: FontWeight.bold),))),);
  }
}